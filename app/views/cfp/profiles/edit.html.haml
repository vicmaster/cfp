%h1= t('profile.profile')

.row
  .span5
    = form_for @profile, :url => profile_path do |form|
      = render 'form', :form => form

      .form-actions
        = form.submit :class => 'btn btn-primary'
